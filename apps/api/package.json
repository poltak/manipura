{
  "name": "manipura-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "test": "vitest",
    "test:unit": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^2.1.1",
    "@types/node": "^20.11.0"
  }
}
